## What does this change?

<!-- Describe the viewer improvement (bug fix / feature) here. -->

## Contributor checklist

- [ ] This PR changes the **viewer code/docs only** (`main.js`, HTML, README, etc.).
- [ ] It contains **no model data** — nothing under `resources/models/` and **not** my
      personal `Config.yml`. (Model data belongs on my fork; see CONTRIBUTING.md.)
- [ ] Branch is based on `upstream/main`.
- [ ] The sample model still loads locally.
